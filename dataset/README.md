# Dataset (local only)

The dataset CSVs are not committed because they are very large (GitHub file size limit is 100MB).

Expected file (default):

- `dataset/master_combined.csv`

If you want the apps to use a different location, update `DATASET_PATH` in the app scripts.

